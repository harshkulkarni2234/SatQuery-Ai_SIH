# Test-only dependencies — not needed to run the app, only to run the test
# suite (fastapi.testclient.TestClient requires httpx). Found missing during
# Phase A9's fresh-clone rehearsal: `pip install -r requirements.txt` alone
# left `pytest -q` failing with "No module named pytest" for a first-time
# setup following the README verbatim.
pytest
httpx

# Solo build progress

This repo's build was originally scoped as a 3-person parallel plan
(`SatQuery_Team_Prompts.md`, roles A/B/C, waves 1-4 + final). It is being
executed solo, one phase at a time, in wave order, resuming here whenever a
session restarts. Each row is updated the moment a phase's acceptance
criteria are met and its commit lands.

Legend: `done` / `in_progress` / `blocked` / `todo`

## Day 0

| Phase | Status | Commit | Notes |
|---|---|---|---|
| A0 — Baseline freeze + shared contracts | done | `5b5bf3a` | Tag `baseline-round2` on `f1f7513`. `AGENTS.md`, `docs/CONTRACTS.md`, `docs/BASELINE.md`, `backend/app/contracts.py`, `backend/tests/test_contracts.py` added. 111 backend tests pass, frontend builds clean. Had to rebuild `backend/venv_mac/` (mac venv; committed `venv/` is Windows) and reinstall `frontend/node_modules` (missing `@rollup/rollup-darwin-arm64` optional dep) — see docs/BASELINE.md. |

## Wave 1

| Phase | Status | Commit | Notes |
|---|---|---|---|
| A1 — Real GeoTIFF/TIFF raster ingestion | done | (pending) | `backend/app/services/raster_ingest.py`: `extract_metadata()` (GeoTIFF via rasterio incl. bounds_wgs84 reprojection, TIFF-tag acquisition date with user-date override, PNG/JPG/BMP via Pillow, JP2 attempted via rasterio with graceful degradation, MAX_RASTER_PIXELS guard, magic-byte validation via actual rasterio/Pillow open not just extension) and `load_rgb_preview()` (percentile stretch for 16-bit, band 4/3/2 selection for band_count>=4, downsampling). 14 new tests, all synthetic (rasterio in-memory-style tmp files) + verified manually against a realistic synthetic 4-band uint16 GeoTIFF. rasterio added to requirements.txt (backend stays torch-free). DB untouched (that's A2). |
| A2 — Persist metadata + expose in API (GATE G1) | done | (pending) | Alembic migration `93227407ec1b` adds width/height/band_count/dtype/bounds/bounds_wgs84/transform/nodata/file_format/is_georeferenced/acquisition_date_source/file_size_bytes/metadata_warnings to `images`, widens `crs` to String(255). `models.py`, `schemas.py` (`ImageUploadResponse.metadata`, new `ImageDetailResponse`), `routes/images.py` (calls `extract_metadata` on upload, deletes file + returns 400 on `RasterIngestError`, new `GET /images/{id}`) updated. 4 new tests (`test_images_metadata.py`): real GeoTIFF upload returns full metadata in response+DB, corrupt upload -> 400 + no row, GET works, unknown id -> 404. 129 backend tests pass. **GATE G1 reached** — real image metadata now in the API.|
| B1 — Genuine demo & evaluation data | done (demo scope) | (pending) | User supplied a `testing/` folder (132MB, no manifest/license/README) which I inventoried fully: ~4,000 BigEarthNet-style Sentinel-2 patches (2 different tiles/dates, optical-only, no labels file), exactly 1 genuine co-registered S1+S2 pair (same tile+patch-grid+date), and ~13 misc single demo images of mixed/uncertain provenance (one — farm1.jpg — excluded for a visible DigitalGlobe watermark/licensing risk, per user decision). No real before/after same-location pair existed anywhere in it, so I fetched one myself (user-approved): `ml/data/download_lakemead_temporal_pair.py` pulls 2 real low-cloud Sentinel-2 scenes (2018-08-24, 2022-08-23) of Lake Mead/Hoover Dam from the public AWS sentinel-cogs bucket via STAC + a windowed COG read (~6MB transferred, not the full scene). Built `data/demo/{scenario_A_single,scenario_B_temporal,scenario_C_optical_sar}/` each with a README documenting exact source/license/citation or explicitly flagging it as unconfirmed (per user decision to use unverified-source images with honest labeling), plus `data/manifest.json`. All 3 scenarios verified end-to-end through the real `/images/upload` + `/query` API (VQA, real-measured 5.59% change detection, cross-modal fusion all ran correctly). 4 new tests in `test_demo_data.py`; 160 backend tests pass. `testing/` added to `.gitignore` (132MB raw material, not committed). **Scope note:** this covers the 3 demo scenarios (acceptance criterion for B1); the larger eval-scale datasets B3-B6 need (RSVQA/CDVQA/VRSBench/full BigEarthNet-MM-with-labels) are separate, larger downloads not yet sourced — will revisit when those phases are reached. |
| B2 — Reproducible BigEarthNet LoRA adaptation | todo | | |
| C1 — Split App.jsx into components | done | (pending) | `App.jsx` 1455 → 210 lines. New structure: `components/icons/index.jsx` (all Icon* + `ic()` helper), `components/input/{ImageCard,InputWorkspace}.jsx`, `components/agent/{AgentSelection,AnalysisReady}.jsx`, `components/results/{GroundingImage,VQAResult,GroundingResult,ChangeResult,CrossModalResult,ResultContent,AnalysisResult,TechnicalDetails}.jsx`, `components/common/{Stat,StatGrid,ToggleChip,ViewerToggles,OverlayImage,ConfidenceMeta}.jsx`, `constants/{specialists,scenarios}.js`, `lib/format.js`. Zero markup/logic changes — verified: `npm run build` 28→50 modules, output bundle byte-identical size (173.90kB→173.91kB), zero warnings; clicked through all 4 screens live in the browser (input → drag-drop upload → preset query → analyze → orbit animation → ready → full result report with visual evidence, answer, confidence, technical details → back to input), no console errors. Added `.claude/launch.json` (frontend dev server) to make future browser verification easy. |
| C2 — Real metadata display on upload | done | (pending) | Since G1 was already merged when this phase started, went straight to real data (mocks still added for offline dev: `src/mocks/metadata.js` + `VITE_USE_MOCKS`). `api.js`: `uploadImage` sends optional `capture_date`, added `getImage()`. Changed upload timing: images now upload as soon as they're added (not deferred to Analyze) so metadata shows immediately; modality/date changes re-upload (no update/delete endpoint exists yet, so the old row is orphaned — documented limitation). `ImageCard` shows format/dimensions/bands/capture-date+source/georeferenced/CRS/resolution/extent(WGS84)/warnings, "Not available" for missing values, and per-card upload errors. Added 2-image "pair summary" (informational only). Analyze button disabled while uploading. Found and fixed a real bug while testing live: `raster_ingest.py`'s corrupt-file error messages leaked the full server-side file path (GDAL/PIL exception text) to the client — sanitized both messages, added regression tests. 130 backend tests pass; verified live in the browser (drag-dropped a real synthetic GeoTIFF through the dev server, saw full real metadata; drag-dropped a corrupt file, saw the clean sanitized error). |

## Wave 2

| Phase | Status | Commit | Notes |
|---|---|---|---|
| A3 — Geospatial compatibility engine | done | (pending) | `backend/app/services/compatibility.py`: `check_temporal_pair` (modality match, date-distinct with WARN not FAIL on missing dates, CRS match, bbox-IoU overlap vs `MIN_OVERLAP` env default 0.5, resolution-ratio WARN >1.5x, SKIPPED geo checks when not georeferenced), `check_optical_sar_pair` (same checks + `coregistration`: "verified" only when same CRS and matching transform grid, "assumed" behind an opt-in `coregistered_hint` param not yet wired to an upload field — documented gap, "unverified" otherwise), `check_single_image`. Wired into `routes/query.py` before dispatching CHANGE_DETECTION/CROSS_MODAL: FAIL -> 422 with `{message, compatibility}` body; the pre-existing `_temporal_metadata_compatible` same-date short-circuit in `services/router.py` is untouched (still runs first, during classification). 14 unit tests on synthetic dicts (same-date, non-overlapping, different CRS, wrong modality, non-georeferenced, verified co-registered) + 3 new API-level integration tests through real GeoTIFF uploads (non-overlapping pair -> 422 with report; overlapping pair -> 200; optical+SAR overlapping -> 200). 147 backend tests pass. |
| A4 — Specialist registry (GATE G2) | done | (pending) | `backend/app/services/registry.py`: 6 `SpecialistSpec` entries (vqa.smolvlm_base, vqa.smolvlm_bigearthnet_lora_stage3 — documentation-only, see note below, grounding.deterministic_cv, change.semantic_model — unavailable until B8, change.deterministic_cv — is_fallback, cross_modal.feature_fusion), `list_specialists()`, `candidates_for()`, `select()` (priority order, honest fallback reasons, still returns the sole VQA spec even when its health probe fails since there's no VQA fallback — the handler's own honest unavailability path then runs). `registry_adapters.py` wraps `vqa/grounding/change_detection/cross_modal` (unedited) into `SpecialistResult` without touching those files. New `GET /specialists` (routes/specialists.py). `routes/query.py` refactored: every task branch now calls `registry.select()` + `entry.handler(...)` instead of importing/calling the specialist functions directly; response `metadata` gains `specialist_id`, `selection_reason`, `rejected_specialists`, `used_fallback`, `confidence_source`, `warnings` (all additive — every pre-existing metadata field is unchanged). Found `contracts.SpecialistResult.model_version` was non-optional, which couldn't honestly represent "no model version" (e.g. VQA worker down); widened to `Optional[str] = None`. 8 existing tests patched `query_module.answer_question` (an implementation detail that moved with this refactor) — updated to patch `vqa_module.answer_question` at its real call site; behavior/assertions unchanged. 156 backend tests pass (147 prior + 8 new registry unit tests + 1 new /specialists route test). Verified live via curl end-to-end: GROUNDING and CHANGE_DETECTION (semantic-model-unavailable fallback) both show correct real registry metadata; browser click-through of the grounding flow confirmed no regression. |
| B3 — Evaluation framework scaffold | todo | | |
| B4 — RSVQA runner | todo | | |
| B5 — CDVQA runner | todo | | |
| C3 — Honest execution trace timeline | done | (pending) | Since G3 was already merged when this phase started, wired the real field directly (no mock scaffolding needed). Removed the old fixed-4-second orbit animation that cycled through all 4 specialist icons while waiting (found this was a real honesty-rule violation — AGENTS.md rule 4 says the UI must never imply a model executed unless the backend says it did, and cycling through "trying" all 4 specialists during the wait implied evaluation that never happened; only one deterministic classification occurred, instantly). New flow: `AgentSelection.jsx` is now a neutral "Analyzing your query…" spinner shown for however long the real request actually takes (no fixed clock); new `TraceReplay.jsx` then replays the backend's own `trace_events` one at a time (~220ms/step, capped, labeled "Replay of recorded execution"), each with a real status icon, detail text and real `duration_ms` — failed steps render in red. Shows the real selected specialist (`metadata.plan.selected_specialist_id`), rejected specialists greyed with their real rejection reason as a tooltip (`metadata.plan.rejected_specialists`), and a "Used fallback: `<reason>`" badge when `used_fallback` is true. Removed ~340 lines of now-dead orbit/winner-reveal CSS and the `ORBIT_SEQUENCE`/`ORBIT_POS`/`STEP_LABELS`/`SPECIALIST_DWELL_MS` constants (CSS bundle shrank 23.43kB→20.77kB). No SSE (A6 skipped it; UI replays the real recorded list as CONTRACTS.md always said). Verified live in the browser: since screenshot timing in this environment couldn't reliably catch the ~2s transient states, verified via direct DOM inspection instead (staged an artificial `fetch` delay on `/query` to hold each state open) — confirmed the neutral waiting screen shows no specialist cycling, the trace replay progressively reveals real steps starting with \"Query received\", and a real change-detection query correctly showed \"Used fallback: Selected change.deterministic_cv (fallback)...\" with \"semantic model\" greyed out and tooltipped \"currently unavailable\" — matching this phase's acceptance criterion exactly. 180 backend tests still pass; frontend builds clean. |
| C4 — Validation & compatibility UX | done | (pending) | New `common/CompatibilityChecklist.jsx` (PASS/WARN/FAIL/SKIPPED colored checklist + overlap ratio + co-registration status) and `common/ErrorPanel.jsx` (message + suggestion + checklist). Found `api.js`'s `extractDetail()` silently collapsed the backend's real `{message, suggestion, compatibility}` error body into a generic "Request failed with status 422" — fixed: `ApiError` now carries the raw structured `.detail` alongside a plain-string `.message`, so `ErrorPanel` can render the real reason instead of a useless fallback. `InputWorkspace` now disables Analyze on an empty query too (previously only while uploading), and "Try an example" filters to the 1-2 examples that actually apply to the currently staged images (1 image / 2 same-modality / optical+SAR) via a new `imageCombo()` helper, instead of always showing all 4 regardless of relevance. Note: the actual error shape is `{message, suggestion, compatibility}`, not the `{detail, code, suggestion, trace_events}` shape the phase prompt described — `code` is A8 hardening's job, not yet added; built against what A3/A5 actually ship today. Verified live in the browser: uploaded a genuinely non-overlapping GeoTIFF pair, got a real 422, and the UI rendered the exact backend detail — 5 real checks (modality match PASS, date distinct WARN, CRS match PASS, overlap FAIL "0.00 is below minimum 0.5", resolution ratio PASS) with correct color coding; confirmed contextual examples and the empty-query disable both work. 180 backend tests unaffected (frontend-only phase); frontend builds clean (54 modules). |

## Wave 3

| Phase | Status | Commit | Notes |
|---|---|---|---|
| A5 — Agent planner + deterministic validator | done | (pending) | `backend/app/services/planner.py`: `build_plan()` orchestrates classify_query (via a pluggable `IntentClassifier` Protocol, keyword implementation as the only/default one — no internet LLM) -> structured `question_type` inference (presence/count/description/location/change/comparison) -> compatibility check (single-image/temporal/optical+SAR, whichever applies) -> `registry.select()` -> whitelisted parameter configuration, returning a `PlanResult(plan: ExecutionPlan, selected_entry)`. New `ExecutionPlan` contract in `contracts.py`. `routes/query.py` now calls `build_plan()` once per query instead of separately calling `classify_query` + inline compatibility checks + `registry.select()` in each task branch (removed ~25 lines of duplication); every validation failure returns `{message, suggestion, compatibility?}` instead of a bare string (3 existing tests updated to read `detail["message"]` instead of `detail` directly — deliberate improvement per this phase's own spec, not an accidental break). `metadata.plan` is now in every successful `/query` response and matches exactly what executed (verified live via curl: GROUNDING plan's `selected_specialist_id` matches the specialist that actually ran). 14 new planner tests (empty query, unsupported grounding target, wrong image count for grounding/change-detection, same-date fast-reject vs. non-overlapping-footprint compatibility-report reject, cross-modal, VQA question-type inference, ambiguous 2-image query). 174 backend tests pass; frontend build unaffected. |
| A6 — Event-driven execution trace (GATE G3) | done | (pending) | `backend/app/services/trace.py`: `TraceRecorder` with real `time.perf_counter()`-measured durations, no simulated timing. Threaded through `planner.build_plan()` (INPUT_VALIDATION, TASK_SELECTED, COMPATIBILITY, SPECIALIST_SELECTED) and `routes/query.py` (QUERY_RECEIVED, EXECUTION, EVIDENCE, RESULT, ERROR). Migration `3412d6ca3ee1` adds `trace_events`/`compatibility`/`confidence_source`/`warnings`/`used_fallback`/`specialist_id` to `queries`. **Failures are now persisted too** (previously validation/compatibility/execution failures raised straight to the client with nothing saved) — a `Query` row (no `QueryResult`, there's no answer) is written with the trace ending in an `ERROR` step before the exception propagates, for every failure path: unknown image id (404), empty/invalid query (400), incompatible pair (422), specialist exception (400). `QueryResponse` gains `trace_events`, `compatibility`, `confidence_source`, `warnings`, `used_fallback` (additive; old fields unchanged); `GET /query/{id}` returns the persisted values. SSE (`GET /query/{id}/events`) skipped as explicitly optional in the phase spec — the UI replays the real recorded list instead, as CONTRACTS.md already said. 6 new tests (`test_trace.py`: event ordering, non-negative durations, ERROR-step-on-failure persistence for both the empty-query and unknown-image-id paths) + 174 pre-existing all still pass = 180 total. Verified live via curl: a real GROUNDING query returns 8 ordered trace events with genuine per-step durations (0-6ms) matching exactly what executed. **GATE G3 reached** — every successful query response contains a real trace, and failures are now auditable too. Unblocks C3. |
| B6 — VRSBench runner | todo | | |
| B7 — Raster-aware change detection (partial — see scope note) | done (change_detection scope) | (pending) | `change_detection.py`: real GeoTIFF/16-bit-aware loading via `raster_ingest.load_rgb_preview` (replaces `cv2.imread`, which silently mishandled multi-band/16-bit Sentinel-style rasters); real **geographic-grid reprojection alignment** via `rasterio.warp.reproject` when both images are georeferenced (falls back to the existing phase-correlation registration on any failure or when not georeferenced) — `alignment_method` (`geographic_reprojection`/`phase_correlation`/`none`) is now always reported honestly; real **changed-area-in-m²** computed from actual resolution (never estimated when resolution is unknown — verified by a dedicated test); quadrant location of the largest change region added to the answer text. All of this surfaces through `registry_adapters.py` → `routes/query.py` → `metadata.change_percentage/changed_area_m2/alignment_method` unchanged in shape, additive only. 6 new tests (`test_change_detection_geo.py`: geographic alignment triggers correctly, pixel fallback when not georeferenced, real area-in-m² matches `changed_pixels × resolution`, area is `None` when resolution unknown — and confirmed absent from the answer text, graceful fallback when reprojection raises, and **the real Lake Mead demo pair from B1** producing a real, internally-consistent result). 186 backend tests pass. Verified live via the real API against the actual Lake Mead pair: real answer text now reads \"*The AFTER image was reprojected onto the BEFORE image's geographic grid... This corresponds to approximately 7,776,900 m². The largest change is located in the upper-left...*\" — all real, measured values.<br>**Scope note (honest, not silently dropped):** B7's other explicit item — converting `vqa.py`/`grounding.py`/`cross_modal.py`/`change_detection.py` to return `contracts.SpecialistResult` *natively* (removing the `registry_adapters.py` wrapper layer) — was **not done this pass**. That's a valuable but separate, mechanically risky refactor (36+ direct call sites across 4 test files, plus `registry.py`/`routes/query.py` wiring) that didn't fit safely alongside the scientific upgrades above at the end of an already long session. The adapter layer already produces correct `SpecialistResult` objects at the API boundary today, so nothing is broken — this is deferred architecture cleanup, tracked here for a future pass (would be A7's job per the original plan: \"Remove adapters that are now unnecessary\"). NDVI/NDWI (item 2's other half) was also skipped: none of this repo's actual demo/test assets have NIR/SWIR bands to compute it from honestly, so it would be undemonstrable with real data — cut per the master plan's own \"if time runs short\" guidance rather than built as an unverifiable stub. |
| B8 — Semantic change specialist | todo | | |
| C5 — Downloadable PDF report (backend) | done | (pending) | `backend/app/services/report.py`: `build_report_data()` reconstructs everything from the **stored** query/result/image rows (no re-running any specialist) — title/report-id/generated-time, query text, inputs table (filename/modality/capture-date+source/CRS/resolution/extent/bands, reusing `routes/images.py`'s existing metadata reconstruction), compatibility checklist, task + specialist (name/kind/version/RS-adapted, looked up from `registry.list_specialists()`), fallback+reason, answer, visual evidence (input thumbnails + change mask/overlay, all missing-file-safe — "(evidence image not available)" instead of crashing), confidence+source, full execution-trace table, warnings, and a fixed limitations section (pixel-level≠semantic, co-registration honesty, deterministic-vs-learned). `render_report_pdf()` (reportlab) and `GET /query/{id}/report.json` both consume the exact same `build_report_data()` output, so they can never disagree. New `routes/report.py` (`report.pdf`, `report.json`), registered in `main.py`. Found and fixed a real rendering bug while checking the actual output: long detail text in the Compatibility/Execution-Trace tables overflowed into the neighboring column (plain strings don't wrap in reportlab `Table` cells) — fixed by wrapping every table cell in a `Paragraph`, verified by rendering the PDF to PNG via poppler and inspecting it directly. 5 new tests (`test_report.py`: real `%PDF` bytes, JSON field shape, 404 for unknown id, missing-evidence-file graceful handling); 191 backend tests pass. Verified live end-to-end against the real Lake Mead demo pair — the generated PDF/JSON carry the real change-detection answer (with the B7 upgrades: alignment method, area in m², quadrant location), real compatibility checks, real trace, real specialist metadata. `reportlab` added to `requirements.txt`. |
| C6 — Report download + evidence/confidence standard in UI | done | (pending) | "Download report (PDF)" + "Download JSON" buttons on `AnalysisResult.jsx` linking directly to C5's real endpoints. New `lib/useSpecialists.js` (cached `GET /specialists` lookup) drives a specialist badge ("Learned model"/"Deterministic CV"/"RS-adapted (…, narrow)") next to the result header. `ConfidenceMeta` now shows "value — source: X" or "Unavailable" (previously just a bare number or "Unavailable for this analysis", with no source at all) across all 4 result components. New `WarningsBanner` (always visible when `result.warnings` is non-empty — previously not surfaced anywhere in the UI at all). Also fixed a real, honest gap while wiring the "Report" ID field: it was a client-side hash of task+answer_text with no connection to anything real; now computed from the actual `query_id` with the exact same formula `backend/app/services/report.py` uses, so the ID shown on screen is the same one on the downloaded report. **Found and fixed a real integration bug while testing live:** `frontend/vite.config.js`'s dev proxy never listed `/specialists` (added in A4) — every `GET /specialists` call from the dev server was silently swallowed by Vite's SPA fallback and returned `index.html` instead of JSON, so the specialist badge could never have worked in dev without this fix. 191 backend tests unaffected (frontend-only phase); frontend builds clean (56 modules). Verified live: uploaded an image, ran a grounding query, confirmed the "Deterministic CV" badge, real report ID matching backend format, both download buttons producing a real fetchable `%PDF`/JSON response through the actual UI links, and "1.000 — source: bbox fill ratio" confidence display. |
| C7 — Cross-modal + change result UI upgrades | done | (pending) | `ChangeResult.jsx`: real "Changed area (m²)" stat and "Alignment method" (from B7), plus a "Semantic model used" / "Pixel-level deterministic method" badge derived from the registry lookup (always the latter today, honestly, since no semantic model is selectable yet); removed the now-redundant `registration_applied` note (fully subsumed by the new alignment-method stat). `CrossModalResult.jsx`: co-registration status banner (verified/assumed/unverified/failed, from `result.compatibility.coregistration`) and a modality-attribution legend (optical-only/SAR-only/combined, noting "(none produced)" honestly when a view has no data — per-class water/vegetation/built-up masks were skipped since the backend doesn't actually produce those as separate mask images, only region stats, so there was nothing real to render there). New `InputMetadataPanel.jsx` shows modality/date/CRS/resolution/extent for every input directly on the results page (previously only visible during upload, not carried through to the report view). Verified responsive layout at projector resolution (1280×720). **Found and fixed a real, pre-existing bug while testing cross-modal live:** `App.jsx`'s `updateImage()` (written back in C2) tried to capture the updated item from inside the `setImages` functional updater and read it immediately afterward — but that updater callback runs asynchronously, not synchronously, so the captured value was always `null` and the modality-change re-upload silently never fired. This meant changing an image's modality after upload (e.g. correcting the auto-defaulted OPTICAL to SAR for a cross-modal query) never actually re-uploaded with the new modality, so cross-modal analysis was unreachable through the UI for any image not uploaded with the right modality from the start — confirmed via a direct API call proving the backend classified correctly while the UI silently sent the stale image. Fixed by reading the current item from the `images` render closure directly instead of relying on the updater's synchronous execution. 191 backend tests unaffected (frontend-only phase); frontend builds clean (57 modules). Re-verified end-to-end after the fix: modality change now triggers a real re-upload, and the full cross-modal flow (input → waiting → trace replay → ready → result) correctly classifies as CROSS_MODAL and renders the new banner/legend. |

## Wave 4

| Phase | Status | Commit | Notes |
|---|---|---|---|
| A7 — Integrate B's upgraded specialists | todo | | WAIT for B7, B8, B9 |
| A8 — Backend hardening | todo | | |
| B9 — Optical + SAR co-registered fusion | todo | | |
| B10 — Full benchmark runs + results | todo | | WAIT for A7 |
| C8 — Hardening: API + UI edge cases | todo | | |
| C9 — Demo scenarios, runbook, README | todo | | WAIT for B1 |

## Final

| Phase | Status | Commit | Notes |
|---|---|---|---|
| A9 — Cold-start & final integration | todo | | |
| C10 — Final cold-start rehearsal | todo | | |

## Environment notes (persist across sessions)

- macOS (Darwin, arm64), Python 3.11.7, Node v24.7.0, PostgreSQL 17.11 (Homebrew).
- Backend venv: use `backend/venv_mac/` (created fresh — the committed
  `backend/venv/` is a Windows venv and won't run here). Not committed
  (gitignored via existing venv ignore rule — verify before assuming).
- DB: local Postgres role `shlok`, no password, DB name `satquery`, created
  with `createdb satquery`. `backend/.env` `DATABASE_URL` was changed from
  the original `postgres:iharsh@...` to `postgresql://shlok@localhost:5432/satquery`
  locally (`.env` is gitignored, this edit is not part of any commit).
- Frontend: `frontend/node_modules` was reinstalled from scratch
  (`rm -rf node_modules package-lock.json && npm install`) to fix a missing
  `@rollup/rollup-darwin-arm64` optional dependency. `package-lock.json` was
  regenerated on this platform — check `git diff` on it before assuming it's
  identical to the Windows-generated lockfile.
- Test command: `cd backend && venv_mac/bin/python -m pytest -q`
- Build command: `cd frontend && npm run build`
- No dedicated GPU on this machine for Phase B training work (B2, B8) — see
  notes recorded in each phase's row once reached; will follow the plan's
  honesty rule (no fabricated benchmark numbers) and document real
  hardware/limitations rather than pretending otherwise.

## Deviations from the original plan

- Executed solo instead of 3 parallel people — same file-ownership
  boundaries and wave order are kept for clarity, but there's no daily sync;
  this file is that sync record instead.
- Git workflow simplified: phases are done as small commits directly (still
  following the `feat(<phase-id>): ...` message convention) rather than
  short-lived per-phase branches, since there's no concurrent second author
  to rebase against. Will branch if/when asked to open PRs.
